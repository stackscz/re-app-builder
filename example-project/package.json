{
 "scripts": {
  "ab": "node ./node_modules/re-app-builder/bin",
  "clean": "rm -rf public/build",
  "dev": "npm run clean && NODE_ENV=development npm run ab dev",
  "build": "npm run clean && NODE_ENV=development npm run ab build",
  "build:prod": "npm run clean && NODE_ENV=production npm run ab build"
 },
 "devDependencies": {
  "re-app-builder": "file:../"
 },
 "dependencies": {
  "react": "^15.4.2",
  "react-dom": "^15.4.2",
  "recompose": "^0.22.0"
 }
}
